global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: aegis-edge-reference
    honor_timestamps: true
    static_configs:
      - targets:
          - 127.0.0.1:9464
        labels:
          environment: simulation
